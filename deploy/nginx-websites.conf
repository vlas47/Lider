# Include inside the liderscan.ru HTTPS server block during provisioning.
# 10 MiB file plus multipart fields; other applications keep their existing limit.
location /sites/ {
    client_max_body_size 12m;
    proxy_pass http://unix:/run/cloud-site/gunicorn.sock;
    include proxy_params;
}
